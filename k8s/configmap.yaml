apiVersion: v1
kind: ConfigMap
metadata:
  name: wms-config
  namespace: wms
data:
  # Backend Configuration
  PORT: "5000"
  NODE_ENV: "production"
  
  # Base URL (will be updated after getting LoadBalancer IP)
  BASE_URL: "http://localhost:3000"